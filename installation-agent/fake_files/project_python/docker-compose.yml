version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
  db:
    image: postgres:12
    environment:
      POSTGRES_DB: python_app_db
      POSTGRES_PASSWORD: my_super_secret_hardcoded_pass_123
    ports:
      - "5432:5432"
